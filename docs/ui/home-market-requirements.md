# 首页进度、市场洞察与课程要求核对（2026-09-10）

当前工作区 refactor/frontend-product-shell，基线 38e92fc。本次保留此前修改，未新增 AI 调用。

## 已修复

首页原 nextStep 先检查 match，再检查 diagnosis，直接诊断成功时仍停第三步。现逐步核对当前简历/岗位关联，独立显示完成状态；已生成建议时首页可返回建议，未执行匹配保持未分析。更换版本/岗位不沿用旧组合进度。已生成建议不表示用户已经采纳或保存修改。

市场页改为可见筛选、样本摘要、可点击的前十技能覆盖率、原币种/周期薪资范围、可见词云、折叠来源明细。点击技能只筛选来源明细，不改变全样本统计分母。保留空数据、未知薪资、Mock、来源链接校验与完整技能表。

既有 scripts.import_final_samples --apply 在当前本地库新增 5 条已归档招聘方摘要；不是本次重新采集。当前 API 真实来源 10 条、6 家已知雇主、4 USD/year 可比、5 缺区间、1 周期未知。资历与地区不同，不能作为应届生平均薪资。原五份 Canonical 网页导入入口仍保留。

## 课程要求判断

| 要求 | 当前证据与结论 |
| --- | --- |
| 两款竞品体验、痛点报告 | docs/competitor-analysis.md 与既有报告已归档人工记录；本次没有重复体验。 |
| 5 真实 JD + 3 学生简历、gap | data/holdout/2026-09-08 有完整 JD 和三份作者公开的学生时期简历及来源；不是三份本班授权简历。合成演示简历不计入此数量。 |
| Level 1 | 解析、核对编辑、保存重读、JD 输入、关键词分数与差距已有实现及回归证据。 |
| Level 2 | STAR 与岗位建议已有实现、此前真实验收；本次未复测真实模型。截图中某条原文和建议相同，不能作为有改进的对照样例。 |
| Level 3 | 词云、技能覆盖率、薪资区间和样本观察具备；本次当前库补齐已有市场资料并核对 API。 |
| 答辩展示 | 有演示脚本；仍应实际准备同一简历针对同一 JD 的原始版、人工核实后的优化版，解释改动，不能把生成建议等同已修改简历。 |
| PostgreSQL/pgvector | 既有 final/final-recheck.md 记录通过；当前 Settings 数据库为 SQLite，不把历史 PG 测试冒充当前运行状态。 |

结论：课程功能覆盖基本具备；不能宣称每次 AI 输出都有实质提升或当前运行已满足全部工程部署门槛。旧文档部分仍描述旧前端和默认 provider，应以当前代码与本次实测为准。

## 验证与交付边界

107 项前端测试通过，TypeScript/生产构建通过。实际本地 API 页面在 1440/1280/390px 检查无整页横向溢出/浏览器异常，技能筛选通过；截图保留 .verification/market-*.png。本次没有新增真实模型、PostgreSQL或完整后端全量验收。当前分支既有提交审批待确认，本次未提交推送。
