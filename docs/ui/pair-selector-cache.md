# 统一组合选择与会话缓存（2026-09-10）

匹配分析与 AI 优化共享 PairSelector，统一左简历、右岗位。原地切换已保存对象，列表分页加载，当前选择不在列表时读取单条；读取失败保留选择并提供重试。保留返回简历编辑和岗位管理链接，不改写未保存草稿。

组合改变后，控制器取消旧请求的结果接收，未缓存组合显示待分析状态，不自动调用 AI。成功的匹配（包括综合评估）与诊断按简历 ID/岗位 ID 在当前页面会话中缓存 30 分钟，最多 20 组；恢复时不携带自动诊断意图，失败不进入缓存。刷新清空本轮前端缓存。新简历保存为新 ID，不复用旧版本结果。页面重新生成保持现有显式动作，后端已有的成功评估持久化复用逻辑不变。

前端构建、104 项测试通过：包括两页原地切换、统一顺序、结果恢复且 POST 次数不增加、版本隔离、30 分钟过期、编辑入口和既有过期响应保护。AI 优化隔离浏览器在 1280/1440/1920px 通过并检查截图；所有测试使用合成数据，无真实模型调用。

未提交推送，沿用此前分支审批待确认状态。
