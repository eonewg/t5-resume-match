# 岗位工作区与快速选择验证

2026-09-10，基于当前 `refactor/frontend-product-shell` 工作区实现。

## 用户需求与实现

- 岗位创建独立为 `#/jobs/new`：左侧粘贴、文档提取、截图导入，右侧基本信息、职责与要求、技能与工具分组；支持完整手动填写。
- `#/jobs` 专注已保存岗位库。简历与岗位的编辑/创建、资料库入口分别放在各自工作区内部，不占侧栏独立菜单。
- 匹配和优化使用可搜索的简历、岗位选择器，支持关键字过滤、键盘与鼠标选择、无结果提示。切换仅改变选择与已有结果展示，不自动发起分析。
- 文档提取和 AI 识别分开调用；AI 返回草稿不保存，保留手动字段与有意清空，原文独立备查。跨路由保留草稿，明确保存并重新读取后选中，读取失败只重试读取。

## 实际验证

- `npm test`：72 项 Node 测试和 62 项 React 测试通过；包含文档提取不自动 AI、字段保护、保存失败恢复、保存后读取重试不重复创建、库与创建页分离、草稿保留、搜索技能与无结果、显式选择及分析结果缓存。
- `npm run build`：类型检查与生产构建通过。
- `.venv/Scripts/python.exe -m pytest -q`：687 passed，40 skipped；新增文本识别覆盖 Chat Completions/Responses、原文保留、不写库、文档提取不调用模型、无模型手动保存。
- `.venv/Scripts/ruff.exe check .` 与 `git diff --check` 通过。

本轮模型响应测试使用合成数据和离线传输，未额外调用真实模型；按用户要求未做视觉巡检。跳过项沿用测试环境条件，不计为通过。未合并或发布安装包。

## 标题切换调整

同日按反馈将工作区切换合并至标题：选中项使用大号粗体标题，未选项缩小；标题按实际文字宽度排列并使用统一基线，去掉固定宽度占位。创建页与库页共用相同标题行容器，移除创建页自动聚焦字段引起的滚动。四个页面共用受视口约束的外层布局、页面内滚动、稳定滚动条槽位与一致边距，库页继续保留内部列表滚动。侧栏名称改为「简历」「岗位」。前端现有 134 项测试及生产构建通过，未做视觉巡检。

匹配、AI 优化、市场洞察一并采用相同的工作页容器；首页保留独立布局。统一内容外边距、标题字号/行高、标题到内容的间距，移除页面级标题缩进的影响。市场大屏仍保留全屏布局。
