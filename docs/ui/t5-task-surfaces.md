# T5 任务工作区视觉调整

2026-09-10，分支 `refactor/frontend-product-shell`，以 `f7d3bf4` 为功能基线。

用户否决职业编辑室主题，要求保留本轮功能和后端改动。此次设计只依据 T5 的编辑、选择、分析与修改任务，未查阅其他产品或历史项目的视觉方案。

- 删除首页抽象图、侧栏星形与随机导航符号、英文装饰语、编号式页眉及宋体大标题。
- 使用灰蓝工作区底色，以蓝色表示当前选择和主要操作；边界、错误、待补充内容保留各自语义。
- 首页直接展示下一步、流程状态和当前简历/岗位；缩短页眉和空状态。
- Resume 原文和核对字段共用连续容器，中间分隔线替代独立卡片间距。
- Jobs 使用紧凑的岗位集合，保留搜索、新增入口和当前选择。
- Matching 缩小分数区域；AI 优化以原文/建议对照为主，仅在长段落中使用阅读字体；Analytics 数字使用等宽字体以便比较。
- 保留历史删除、全部清空、关联数据处理、建议去重与全部业务调用。无后端、数据库、AI Prompt、算法或 Windows 打包配置修改。

验证：`npm run build` 通过；`npm test` 的 63 项逻辑测试及 22 项 React 测试通过。初次构建发现页眉组件仍要求旧装饰属性，已删除该过时属性并复验。Vite 在受限环境发生 Windows 子进程 EPERM，获准后在沙箱外运行通过。

用户随后明确取消逐页检查，由用户自行验看视觉。本轮未运行浏览器截图验收，也未重新构建 EXE；生产前端构建仍输出到原 `frontend/dist`。
