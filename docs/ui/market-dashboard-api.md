# 市场数据大屏与外部岗位同步（2026-09-10）

最新界面与统计默认范围见 [Level 3 分析总览对齐](level3-analysis-overview.md)。下文保留早期同步与验收记录。

当前基线：refactor/frontend-product-shell / 38e92fc，保留此前未提交工作。本轮没有新的分支集成；尚未提交推送，沿用此前提交目标待确认状态。

## 实现

- 恢复用户认可的技能柱状图 + TOP 5；来源日期筛选收起，四个图表/解读板块在桌面等高对齐。
- 薪资主图按选定币种/周期，以原薪资区间中点分箱计数。默认样本最多的分组，各岗位原区间可展开；无跨币种换算、无未知值补零、无预测薪资。图区内滚动，避免长表撑开整页。
- 词云和样本解读直接可见；详细技能频率/来源表保留。大屏按钮展开到应用视口，Escape 或退出按钮恢复；手机单列。
- 新增 Jobicy 同步按钮；用户选择不限定市场，先接入可用真实数据。官方 API 不需 Key，手动请求最新最多 200 条，缓存 1 小时，未创建后台定时任务。
- 旧的底部“归档样本 / 数据管理”及五份快照按钮按用户要求移除。保留岗位来源明细和已入库记录，历史 API/课程资料未删除。

## 本次真实证据

官方文档：https://github.com/Jobicy/remote-jobs-api

本次对公开 API 一次 GET 实际 200 响应、200 条记录、110 条有薪资字段。复用该响应缓存导入，199 条成功，1 条（ID 150385）标题超过既有字段上限被跳过。全库真实来源现为 209 条、92 家已知雇主、114 条可比薪资、94 条缺区间、1 条单位未知。保留此前 10 条归档来源；外部岗位主要为全球远程市场，不代表中国大陆或应届生市场。

外部大量英语文本揭示裸 go 的语言歧义。仅在新导入适配中要求 Go 的明确技术语境；不修改 Jobs 公共词表/算法。修正本轮刚导入的 57 条自动识别结果，逐条确认原保存值仍等于本轮导入且无 Match/Diagnosis 关联，未覆盖用户编辑或旧记录。当前 Go 为 14 条，Python 为 37 条；统计仍应按有限词表理解。

实际网页再次点击同步：新增 0、已有 199、跳过 1，使用小时缓存；总样本保持 209，薪资数保持 114。没有额外模型调用。

## 验证

- 前端 110 tests passed；TypeScript 与 Vite build 通过。
- Analytics + Core 160 passed / 33 skipped（需另启用的外部/PG验收）；新增 Go 语境过滤后 Analytics 15 passed。2 条既有依赖弃用提示保留。没有把跳过项记为通过。
- 实际 Edge 1920/1440/1280/390px：无横向溢出/页面异常，桌面图表对齐，大屏展开/Escape 退出、真实同步去重通过。最终截图 `.verification/dashboard-*.png` / `dashboard-wide-*.png`。
- Ruff 与 diff 检查通过；本轮未重新执行 PostgreSQL 或付费模型验收。
- 初次 pytest 临时目录权限受限；一次构建提升请求被审批服务额度拒绝，用户要求继续后正常重试通过。

## 后续桌面范围收口

用户随后明确不再需要移动端。已删除 styles.css/workspaces.css 中 <=1000px 的手机/平板专用断点和导航重排，保留 1200/1250px 桌面窗口适配及减少动画偏好。布局最小宽度 1100px；当前验收为 1920/1440/1366/1280，旧移动端结果仅保留历史事实。浏览器测试统一改为桌面宽度，不再以 390px 为门槛。

最终 110 项前端回归、生产构建和四档桌面实际市场页检查通过；旧快照面板不存在，大屏展开/Escape 及小时缓存同步去重通过。调整了历史浏览器脚本中已失效的“简历页内历史折叠”定位，改为独立历史简历页入口。

桌面主流程浏览器复验完成：1920/1440/1366/1280 四档全部通过，覆盖首页五种进度、历史导航、匹配失败恢复、Mock 隔离、薪资口径切换、外部同步响应 fixture。无真实 AI 调用。历史旧定位导致的首次失败已由独立历史页定位修复；业务断言保留。

## 当前页签、国内招聘与侧栏（后续调整）

四宫格改为技能需求、薪资分析、岗位与来源三个页签，一次展示一个完整主题。技能覆盖率直接展开，点击技能跳到对应来源；保留完整频率和词云。样本解读收起。薪资默认 CNY/月（存在时），原始区间直接展开，横轴 5 个数值刻度与条形共用尺度，每行明确币种/周期、上下限；支持其他口径，不换汇。页签支持方向键、Home、End。

全站侧栏可收起为 76px 图标栏，主内容同步扩展，导航名称仍提供可访问标签和悬停提示；localStorage 保存折叠偏好，减少动画偏好仍有效。

国内公开来源：国家大学生就业服务平台 `https://www.ncss.cn/student/jobs/index.html`。列表请求最多 30 条，本次实际返回 20 条；19 条详情可读取并入库，1 条招聘公告未取得匹配正文而跳过。新增 19 条 CNY/月薪资记录（官方列表 K/月按 1000 转换），真实样本累计 228 条、98 家雇主、133 条可比薪资、94 条缺区间、1 条单位未知。来源为混合职业公开招聘，不代表完整国内市场。同步复验新增 0 / 已有 19 / 跳过 1。没有调用 AI。

前端 111 项测试、市场模块 18 项测试与生产构建通过；Edge 1920/1440/1366/1280 实际页面检查通过，覆盖单一可见页签、5 个薪资刻度、来源表、侧栏宽度与刷新记忆、大屏 Escape 和真实重复同步；无页面异常或横向溢出。截图 `.verification/market-skills-*.png`、`market-salary-*.png`、`market-collapsed-*.png`。本次仍保留在当前工作区，未提交推送。
`shell-boundaries-smoke.cjs` 也在四档桌面宽度通过：首页状态、独立历史导航、匹配恢复、Mock 标识、薪资切换和外部同步 fixture 均正常。隔离测试服务已关闭，本地 8000 服务保留。
