# 成员集成请求

当前无待处理的 B/C/D/E 请求。成员将请求保存为 `<角色>-<主题>.md`，说明现有公开接口、期望契约、原因、调用示例、依赖及验证方式。不要直接更改公共 Schema、路由、数据库或根配置。

A 在同一文件追加处理结论与验证证据，并更新 `docs/api-contract.md`。缺少成员实现时继续使用已标注 Mock；接入验收需提供可导入公开类、依赖清单和固定输入输出样例。
