# 补充 STAR 验证报告

2026-09-08，基于已冻结 A 提交 `ac952ff63e37214392f66fb42fe4446026556c81`。本轮仅执行用户指定的补充效果验证，未修改功能、D Diagnosis、Prompt、UI 或模型参数；不重做原固定评估。

## 输入与执行

调用前固定两组完整项目经历，而不是首条标题/时间。S1：公开学生履历的网页原型与 MySQL 数据库项目，对应含 web/database 要求的 Canonical 岗位；S2：公开学生履历的 Tecorigin 性能优化项目，含既有瓶颈、耗时和加速比，对应含性能要求的毕业生软件岗位。原文逐字摘取，JD 用既有全文；来源、理由、哈希见 [本批材料](../../data/evaluation/star-supplement-2026-09-08/README.md)。这不是新增受试者或完全满足招聘硬条件的证明。

真实配置保持 custom/openai_chat / glm-5.2，单次 30 秒、最多三次服务内部尝试，原 Prompt 不变。每组仅发起一次外层验证，服务既有重试不改动。

## 结果与评价

| 案例 | 执行结果 | 内部尝试 | 总耗时 | 四项效果评价 |
| --- | --- | --- | --- | --- |
| S1 | TemporaryLLMError，请求失败 | 3 | 93.133 秒 | 均无法评价（null） |
| S2 | TemporaryLLMError，请求失败 | 3 | 94.140 秒 | 均无法评价（null） |

没有收到可用诊断结果，因而没有可评价的 STAR。不是“成功响应但无 STAR”，也不能记作忠实、无事实新增或零分。原文忠实度、新增事实/数字、JD 针对性、改写可用性均无证据得分。

独立 Agent 核验固定输入、失败记录及原评估保留情况，详见 [独立核验](../../data/evaluation/star-supplement-2026-09-08/independent-review.json)。这是 AI 证据审查，不是人工体验或成功改写的质量评价。

## 验收结论

**补充验证已执行并如实记录；效果结论仍不可得。** 当前冻结配置暴露真实调用可用性限制。按用户指示不为通过验收修改 Prompt、超时或样本，不再重复请求补成功。两组失败与六次内部尝试完整保留。

原固定评估四个文件哈希在调用前后相同；原两空 STAR/一失败和独立评分不变。本批不合入原固定结果，不计算混合成功率或改写提升。可创建披露上述限制的最终审查 PR，但不能把本报告写成 STAR 质量 PASS。
