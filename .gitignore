.venv/
__pycache__/
.pytest_cache/
*.py[cod]
.env
# Keep local demo databases out; data assets (jd/resumes/baselines) are versioned.
data/*.db
.verification/
# Local course materials remain on disk; not application source.
doc/
# Each member keeps their own role instructions locally.
/AGENTS.md
