# T5 严格需求对照表

> 本项目只按课程选题五「T5：AI 简历诊断与岗位匹配系统」实现。功能优先级以课程要求为准，不自行偏题或替换核心功能。

职责按[五人分工方案](T5_FIVE_PERSON_ALLOCATION.md)安排；本表是验收目标，不表示所有要求已经通过验证。

## 1. 问题定义阶段

| 课程要求 | 项目必须产出 | Owner |
|---|---|---|
| 体验至少 2 款主流招聘 APP 的简历制作与匹配功能 | 竞品分析记录，至少比较简历制作、岗位匹配、诊断建议三个维度 | E 主负责，B/C/D 提供模块视角，A 确认范围 |
| 收集 5 份真实 JD 和 3 份学生简历 | `data/samples/` 或课程允许的数据目录中的脱敏样本 + gap 分析 | E 负责样本管理，B 核对简历，C/D 负责技能/表达 gap 分析 |
| 总结常见“表达差距”和“技能差距” | 《求职市场痛点分析报告》 | E 汇总，C/D 提供算法侧结论，A 复核 |
| 明确系统侧重点 | 报告中明确：系统以“智能匹配 + AI 定向简历优化”为主，市场分析为高阶扩展 | A |

## 2. Level 1 核心目标

### 2.1 结构化简历编辑器

必须实现：

- 用户粘贴简历文本
- 系统自动解析为结构化字段
- 前端以结构化形式展示
- 用户可以修改结构化内容
- 可以保存并再次读取
- 原始文本保留，不能因解析失败而丢失

Owner：B；A 提供公共 API 与存储。

### 2.2 JD 输入

必须实现：

- JD 文本输入框
- JD 文本保存
- 解析技能、工具等关键词
- 允许后续匹配与诊断复用

Owner：C 负责 JD 与解析；A 负责持久化与公共 API。

### 2.3 简单匹配度与差距清单

必须首先有**关键词（技能、工具）匹配基线**，不能只做向量相似度。

输出至少包含：

- 0–100 匹配度分数
- 已匹配技能
- 缺失技能 / gap 清单
- 简短、可解释的评分依据

向量相似度可以作为增强项，与关键词结果组合，但不能替代课程明确要求的关键词匹配。

Owner：C

## 3. Level 2 进阶目标：AI 简历诊断引擎

必须实现两类能力：

### 3.1 内容增强

对一段平淡经历描述：

- 给出 STAR（Situation / Task / Action / Result）优化建议
- 保留事实边界
- 不编造岗位、职责、技能、项目成果或数字
- 缺少量化成果时应提示用户补充，而不是生成虚假数字

### 3.2 针对 JD 的定向优化

输入简历 + JD 后：

- 提示应强化的关键词
- 指出技能或表达缺口
- 建议哪些经历应突出
- 建议补充量化成果的位置和方式
- 不得把建议写成未经用户确认的事实

Owner：D

## 4. Level 3 高阶目标：就业市场分析智能体

聚合系统中已录入 JD，至少生成：

- 近期热门技能词云
- 各岗位薪资分布图
- 各岗位技能要求分布图
- 简要职业规划/市场观察文字说明

实现要求：

- 数据来源必须标记为真实采样、课程样本或演示数据
- 不把少量样本外推成整个就业市场事实
- JD 数据结构中必须能保存/解析薪资信息，无法解析时允许空值
- 图表必须来自已录入 JD 数据

Owner：E 负责 analytics 与图表；C 提供 JD 技能/薪资解析能力；A 提供公共查询。

## 5. 推荐技术栈对齐

课程推荐：

- 界面：Stitch + Figma
- 后端：FastAPI + 纯 Python NLP（轻量规则 + 向量相似度）
- 数据库：PostgreSQL + pgvector
- AI 辅助：Trae / Claude Code / Codex / OpenCode
- JD 数据：公开招聘 JD 人工采样整理或课程样本

项目执行：

- 前端原型：Stitch / Figma；最终 Web 前端可以沿用现有公共壳
- 后端：FastAPI
- NLP：关键词规则为必选基线；向量相似度作为增强
- 数据库：最终交付切换并真实验证 PostgreSQL + pgvector
- AI 开发：Codex 为主要代码 Agent
- 数据：至少 5 份真实 JD + 3 份学生简历，脱敏后用于 gap 分析与演示

## 6. 不得偏题

在 Level 1–3 未完整可演示前，不优先开发与 T5 无直接关系的功能，例如：

- 社交/聊天
- 多租户复杂权限
- 无关推荐系统
- 复杂 Agent 编排
- 非必要微服务拆分
- 与简历/JD/匹配/诊断/市场分析无关的功能

任何新功能都必须回答：“它对应 T5 哪一条要求？”无法对应则默认不做。

## 7. 最终演示主链路

最终答辩必须能现场完成：

1. 粘贴原始简历
2. 自动解析为结构化简历
3. 编辑并保存
4. 输入/选择一份 JD
5. 展示关键词匹配度与技能 gap
6. 调用 AI 诊断
7. 展示 STAR 优化建议与 JD 定向优化建议
8. 对比原始简历与优化后的内容，并解释优化逻辑
9. 进入就业市场分析页
10. 展示热门技能词云、薪资分布、技能要求分布

以上链路优先级高于额外功能。
